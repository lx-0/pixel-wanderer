{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "nodemon src/index.ts",
    "reset": "rm -rf dist && rm -rf node_modules/.vite && npm run reset:node",
    "reset:full": "rm -rf dist && rm -rf node_modules/.vite && npm run reset:node:full",
    "reset:node": "rm -rf node_modules && npm ci",
    "reset:node:full": "rm -rf node_modules && rm package-lock.json && npm cache clean --force && npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.5",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
