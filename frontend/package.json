{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "reset": "rm -rf dist && npm run reset:node",
    "reset:full": "rm -rf dist && npm run reset:node:full",
    "reset:node": "rm -rf node_modules && npm ci",
    "reset:node:full": "rm -rf node_modules && rm package-lock.json && npm cache clean --force && npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.5.5",
    "@vitejs/plugin-legacy": "^5.4.2",
    "typescript": "~5.1.6",
    "vite": "^5.4.7"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "phaser": "^3.85.2"
  }
}
