{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "reset": "rm -rf dist && rm -rf node_modules/.vite",
    "reset:modules": "npm run reset && npm run reset:node",
    "reset:full": "npm run reset && npm run reset:node:full",
    "reset:node": "rm -rf node_modules && npm ci",
    "reset:node:full": "rm -rf node_modules && rm package-lock.json && npm cache clean --force && npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^20",
    "@vitejs/plugin-legacy": "^5.4.2",
    "typescript": "~5.1.6",
    "vite": "^5.4.7"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "phaser": "^3.85.2",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
